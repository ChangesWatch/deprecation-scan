# Contributing

Please open an Issue before a substantial change. Keep parser fixtures minimal and free of private package names or lockfile contents. Run `npm run check` before opening a pull request.

Changes to the public catalog schema, matching semantics, network destinations, permissions, or default failure behavior require a security review and release-note entry.
